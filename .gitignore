# secrets — never commit. Ignore every .env* variant, then re-allow the template.
.env*
!.env.example

# deps & build
node_modules/
dist/
web-dist/

# runtime data
*.sqlite
*.sqlite-*
data/traces/
*.log

# os / editor
.DS_Store
.vscode/
.idea/
