# Copy to .env and fill in. .env is gitignored — never commit a real key.
FIRECRAWL_API_KEY=fc-...

# Optional overrides
# FIRECRAWL_API_BASE=https://api.firecrawl.dev

# Optional AI — a small chat model for sharper query expansion + entity probes.
# That's the ONLY thing AI does here; ranking/dedup/diversity are lexical (fast, no
# embeddings). All opt-in: with none set, the pipeline is fully lexical. Pick ONE.
#
# A) Hosted (no local setup):
# ANTHROPIC_API_KEY=sk-ant-...        # Claude
# ANTHROPIC_MODEL=claude-haiku-4-5
# OPENAI_API_KEY=sk-...               # OpenAI
# OPENAI_MODEL=gpt-4o-mini
#
# B) Local via Ollama (run `make models` to install + pull a small model):
# OLLAMA_HOST=http://localhost:11434
# EXPAND_MODEL=llama3.2:3b            # small + fast — the task is just words + lists
#
# Force a provider when several are set (else: anthropic > openai > ollama):
# LLM_PROVIDER=anthropic

# Hard-disable AI regardless of the above (e.g. a deploy whose box can't serve
# models). The UI AI toggle is greyed out with a tooltip; the server ignores any
# request to use models. Leave unset for local dev.
# AI_DISABLED=1
